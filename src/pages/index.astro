---
import BaseLayout from '../layouts/BaseLayout.astro';
import GameCard from '../components/GameCard.astro';
import SearchBar from '../components/SearchBar.astro';
import { getCollection } from 'astro:content';

const allGames = await getCollection('games');
const featuredSlugs = ['uno', 'loup-garou', 'skull-king', 'president', 'flip-seven', 'the-gang'];
const featuredGames = featuredSlugs
  .map((slug) => allGames.find((game) => game.data.slug === slug))
  .filter((game) => game !== undefined);

// Collect unique categories
const allCategories = [...new Set(allGames.flatMap((game) => game.data.categories))].sort();
---

<BaseLayout title="Règles du Jeu - Règles de jeux de société en français">
  <!-- Hero Section -->
  <section class="relative mx-4 mt-6 overflow-hidden rounded-3xl bg-gradient-to-br from-gray-900 via-violet-950 to-gray-950 text-white sm:mx-6 lg:mx-auto lg:max-w-6xl">
    <div class="absolute inset-0 overflow-hidden opacity-10 pointer-events-none">
      <div class="absolute top-10 left-10 text-8xl">🎲</div>
      <div class="absolute top-20 right-20 text-6xl">♟️</div>
      <div class="absolute bottom-10 left-1/3 text-7xl">🃏</div>
      <div class="absolute bottom-20 right-10 text-5xl">🎯</div>
    </div>
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14 pb-14 sm:pb-20 relative">
      <div class="max-w-2xl mx-auto text-center">
        <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight mb-4">
          Les règles de vos jeux
          <span class="block text-primary-200">expliquées simplement</span>
        </h1>
        <p class="text-lg sm:text-xl text-primary-100 mb-8 leading-relaxed">
          Retrouvez les règles de vos jeux de société préférés, résumées en français clair et accessible.
        </p>

        <!-- Search -->
        <SearchBar />
      </div>
    </div>
  </section>

  <!-- Featured Games -->
  <section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16">
    <div class="flex items-center justify-between mb-8">
      <div>
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-gray-100">Jeux populaires</h2>
        <p class="text-gray-500 dark:text-gray-400 mt-1">Les règles les plus consultées</p>
      </div>
      <a href="/jeux/" class="btn-secondary hidden sm:inline-flex">
        Voir tous les jeux
        <svg class="w-4 h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </a>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {featuredGames.map((game) => (
        <GameCard
          title={game.data.title}
          slug={game.data.slug}
          description={game.data.description}
          players={game.data.players}
          duration={game.data.duration}
          age={game.data.age}
          categories={game.data.categories}
          difficulty={game.data.difficulty}
          image={game.data.image}
        />
      ))}
    </div>

    <div class="mt-8 text-center sm:hidden">
      <a href="/jeux/" class="btn-primary">
        Voir tous les jeux
        <svg class="w-4 h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
        </svg>
      </a>
    </div>
  </section>

  <!-- Categories -->
  <section class="mx-4 rounded-3xl bg-white py-10 dark:bg-gray-900 sm:mx-6 sm:py-12 lg:mx-auto lg:max-w-6xl">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-gray-100">Parcourir par catégorie</h2>
        <p class="text-gray-500 dark:text-gray-400 mt-1">Trouvez des jeux selon vos envies</p>
      </div>

      <div class="flex flex-wrap justify-center gap-3">
        {allCategories.map((category) => (
          <a
            href={`/categories/${category}/`}
            class="px-4 py-2 rounded-full bg-primary-50 text-primary-700 hover:bg-primary-100 dark:bg-violet-500/10 dark:text-violet-300 dark:hover:bg-violet-500/20 font-medium text-sm transition-colors capitalize"
          >
            {category}
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16">
    <div class="bg-gradient-to-r from-primary-50 to-primary-100 dark:from-gray-900 dark:to-gray-800 dark:border dark:border-gray-800 rounded-2xl p-8 sm:p-12 text-center">
      <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-gray-100 mb-3">
        Vous ne trouvez pas votre jeu ?
      </h2>
      <p class="text-gray-600 dark:text-gray-400 mb-6 max-w-lg mx-auto">
        Nous ajoutons régulièrement de nouvelles règles. Consultez tous les jeux disponibles ou revenez bientôt.
      </p>
      <a href="/jeux/" class="btn-primary">
        Explorer tous les jeux
      </a>
    </div>
  </section>
</BaseLayout>
